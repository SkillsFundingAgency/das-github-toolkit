trigger:
  batch: true
  branches:
    include:
      - "*"

variables:
- name: SolutionBaseName
  value: __SolutionBaseName__
- name: BuildConfiguration
  value: release
- name: BuildPlatform
  value: any cpu

pool:
  name: DAS - Continuous Integration
  workspace:
    clean: all

resources:
  repositories:
  - repository: self
  - repository: das-platform-building-blocks
    type: github
    name: SkillsFundingAgency/das-platform-building-blocks
    ref: refs/tags/0.3.31
    endpoint: SkillsFundingAgency

stages:
- stage: QualityAndUnitTests
  jobs:
  - template: pipeline-templates/job/quality-unit-test.yml
- stage: IntegrationTests
  condition: and(succeeded(), or(variables['Build.Reason'], 'PullRequest'), eq(variables['Build.SourceBranch'], 'refs/heads/main')))
  variables:
  - group: RELEASE das-github-toolkit
  jobs:
  - template: pipeline-templates/job/integration-test.yml
